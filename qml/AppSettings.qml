import QtQuick 2.15

// Setările aplicației, momentan doar în memorie (fără persistență/backend încă).
QtObject {
    // "ro" | "en" | "ru" — fără traduceri reale încă, doar selecția e reținută.
    property string language: "ro"
}
